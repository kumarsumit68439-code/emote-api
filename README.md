# emote-api

Free Fire Emote API (Flask)

## Run on Termux

```bash
pkg install python git -y
git clone https://github.com/kumarsumit68439-code/emote-api.git
cd emote-api
pip install -r requirements.txt
python main.py
```

Then open: http://127.0.0.1:5000

Endpoint: `/join`
